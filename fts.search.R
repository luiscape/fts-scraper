#### Function to search the crisis id in FTS -- necesary for fetching the data from its API. ####


fts.search <- function() {
  # Fetch a list of crisis (start with current if easier). Then parse the list looking for the 
  # name of a country. 
  # Should use iso3 code?
}